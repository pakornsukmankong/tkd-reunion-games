import { AppContainer } from '@/component/container'
import Home from '@/component/home'

const HomePage = () => {
  return (
    <AppContainer>
      <Home />
    </AppContainer>
  )
}

export default HomePage
